# Backend_Node

# Server (REQUIRED)
PORT=
BIND_HOST=0.0.0.0

# Database (MySQL)
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3307
MYSQL_USER=
MYSQL_PASSWORD=
MYSQL_DATABASE=

# JWT
JWT_SECRET=
JWT_EXPIRES_IN=7d

# Uploads
UPLOADS_DIR=uploads

# Optional
DEBUG=false

# Redis (REQUIRED)
# System rule: REDIS_prefix must exist (value = root folder name)
REDIS_prefix=
# Compatibility (existing code may reference this)
REDIS_PREFIX=
